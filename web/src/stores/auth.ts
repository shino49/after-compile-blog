import{defineStore}from'pinia';import{api}from'../api';export const useAuth=defineStore('auth',{state:()=>({user:null as null|{username:string},checked:false}),actions:{async check(){try{this.user=await api('/auth/me')}catch{this.user=null}this.checked=true;return!!this.user},async login(username:string,password:string){this.user=await api('/auth/login',{method:'POST',body:JSON.stringify({username,password})})},async logout(){await api('/auth/logout',{method:'POST'});this.user=null}}})
