export class ApiError extends Error{constructor(message:string,public status:number,public fields:Record<string,string>={}){super(message)}}
export async function api<T>(url:string,init?:RequestInit):Promise<T>{const res=await fetch('/api'+url,{...init,headers:{'Content-Type':'application/json',...(init?.headers||{})}});if(!res.ok){const x=await res.json().catch(()=>({error:{message:'网络请求失败'}}));if(res.status===401&&location.pathname.startsWith('/admin')&&!location.pathname.endsWith('/login')) sessionStorage.setItem('authExpired','1');throw new ApiError(x.error?.message||'请求失败',res.status,x.error?.fields)}if(res.status===204)return undefined as T;return res.json()}
